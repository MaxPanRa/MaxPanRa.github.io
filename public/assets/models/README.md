Place test models here.

The component already includes these asset paths in its model selector:

- `/assets/models/bottle.glb`
- `/assets/models/flask.obj`
- `/assets/models/smallEarCup.stl`

If a selected file is missing, the preview falls back to the procedural test bottle.
